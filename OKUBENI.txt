═══════════════════════════════════════════════════════════════
  SENTRY V8 GERÇEK FIX
  4 Mayıs 2026
═══════════════════════════════════════════════════════════════

✱ NEDEN ÖNCEKİ ÇÖZÜMLER ÇALIŞMADI
─────────────────────────────────

İlk versiyonda 2 hata yaptım, sonunda buldum:

1. instrumentation-client.ts → Next.js 15+ özelliği. Next 14 
   tanımıyor. SİLİNDİ.

2. next.config.js'te withSentryConfig 3 ARGÜMAN ile çağrılmıştı:
     withSentryConfig(nextConfig, sentryWebpackOpts, sentrySdkOpts)
   
   Bu Sentry V7 sözdizimi. V8'de DESTEKLENMİYOR.
   V8 migration guide:
   > "withSentryConfig will no longer accept 3 arguments"
   
   Bu yüzden SDK options yok sayıldı, build sırasında 
   sentry.client.config.ts otomatik enjeksiyon devreye girmedi.

✱ ÇÖZÜM
───────

next.config.js V8 formatına çevrildi:
  withSentryConfig(nextConfig, { 
    org, project, authToken, silent,
    widenClientFileUpload, hideSourceMaps, 
    disableLogger, tunnelRoute, ... 
  })  // TEK obje, 2 argüman

Şimdi withSentryConfig browser bundle'a sentry.client.config.ts 
otomatik enjekte ediyor. Sentry.init() çağrılır, DSN doldurulur.

✱ DOSYALAR (2 değişen + 1 silinecek)
─────────────────────────────────────

DEĞİŞEN:
  next.config.js                 V8 format - 2 argümanlı
  sentry.client.config.ts        (Önceki versiyon silindiği için
                                  yeniden eklendi - aynı içerik)

SİLİNECEK (eğer sende varsa):
  instrumentation-client.ts      Next 14 desteklemiyor
  
  Yoksa zaten silmemişsin demektir, sorun değil.

DEĞİŞMEYENLER (zaten doğru):
  sentry.server.config.ts
  sentry.edge.config.ts
  instrumentation.ts
  package.json (@sentry/nextjs ^8.0.0)

✱ PUSH
──────
1) Eğer instrumentation-client.ts varsa SİL:
   del instrumentation-client.ts
   (veya: rm instrumentation-client.ts)

2) Git'e ekle:
   git add next.config.js
   git add sentry.client.config.ts
   git rm instrumentation-client.ts  (eğer git'te varsa)
   
   git commit -m "fix(sentry): v8 withSentryConfig 2-arg format"
   git push

VEYA tek komut:
   del instrumentation-client.ts
   git add .
   git commit -m "fix(sentry): v8 withSentryConfig 2-arg format"
   git push

✱ DEPLOY VE TEST
────────────────
1) Vercel auto-deploy bitince
2) Hard refresh (Ctrl+Shift+R)
3) F12 → Console:

   window.__SENTRY__?.hub?.getClient()?.getDsn()
   
   ARTIK DSN object dönmeli. undefined DEĞİL!
   Örnek:
   {
     host: "o4511331240509520.ingest.us.sentry.io",
     pathname: "/4511331240509520",
     protocol: "https",
     publicKey: "abc123...",
     ...
   }

4) Test error fırlat:
   throw new Error('TEST-' + Date.now())

5) 30sn içinde Sentry → Feed → hata gelir

═══════════════════════════════════════════════════════════════
