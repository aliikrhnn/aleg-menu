SPRINT 1A FIX 3 — recordUsage signature uyumu

Sorun:
  recordUsage(businessId, feature) çağırmıştım.
  Mevcut signature: recordUsage(businessId, userId, feature, tokensUsed?)

Düzeltme:
  - requireBusinessAccess()'ten user'ı destructure ettim
  - recordUsage(businessId, user.id, 'nutrition')

Sadece 1 dosya:
  lib/actions/ai.ts

Push:
  git add lib/actions/ai.ts
  git commit -m "fix: recordUsage signature with userId"
  git push origin main
