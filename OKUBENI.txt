ALEG VARDİYA — BUILD HOTFIX
==============================

SORUN
======
Pre-push hook'ta build hatası:
  "Server actions must be async functions"
  
SEBEP
======
'use server' dosyalarda SADECE async fonksiyonlar export edilebilir
(Next.js kuralı). lib/actions/staff.ts içinde:
  - export const ROLE_COLORS  ❌ (sabit, async değil)
  - export function getRoleColor()  ❌ (senkron fonksiyon)

TYPE export'ları sorun değil (compile'da silinir) ama runtime
const/function export'u hata veriyor.


YAPILDI
========

[1] lib/staff-constants.ts (YENİ)
   'use server' yok → her şey export edilebilir
   • StaffRole, ShiftTemplateKey, ShiftCellValue tip'leri
   • ROLE_COLORS, ROLE_LABELS, ROLE_KEYS sabitleri
   • SHIFT_COLORS, SHIFT_LABELS sabitleri
   • getRoleColor() helper

[2] lib/actions/staff.ts (üzerine)
   • Sabit ve helper KALDIRILDI
   • lib/staff-constants'tan import edip type re-export

[3] lib/actions/shifts.ts (üzerine)
   • Type'lar lib/staff-constants'tan re-export
   • Sadece async fonksiyonlar kaldı (zaten öyleydi, temizlik için)

[4] app/panel/(shell)/vardiya/staff-list.tsx (üzerine)
   • Import iki kaynaktan:
     - lib/actions/staff: server actions (async)
     - lib/staff-constants: getRoleColor + sabit'ler
   • Component içindeki duplicate ROLE_LABELS/ROLES kaldırıldı,
     constants'tan kullanıyor


PAKET DEĞİŞMİYOR (npm install GEREKMEZ)


KURULUM
========

4 dosya (1 yeni + 3 üzerine):
  lib/staff-constants.ts                      (YENİ)
  lib/actions/staff.ts                        (üzerine)
  lib/actions/shifts.ts                       (üzerine)
  app/panel/(shell)/vardiya/staff-list.tsx    (üzerine)


PUSH (PowerShell)
==================

  git add .
  git commit -m "fix: vardiya server actions - sabit'leri lib/staff-constants'a tasi"
  git push origin main


TEST
=====

[A] BUILD GEÇİYOR MU:
    npm run build  → ✓ Compiled successfully
    pre-push hook artık geçer

[B] PERSONEL EKRANI:
    panel.alegstudio.com/vardiya → aynı şekilde çalışmalı
    (sadece refactor, davranış değişmedi)
