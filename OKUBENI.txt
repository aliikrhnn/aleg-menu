═══════════════════════════════════════════════════════════════
  ALEG · SENTRY TYPE FIX
  3 Mayıs 2026
═══════════════════════════════════════════════════════════════

✱ NE OLDU
─────────
sentry.server.config.ts'de cookies'e string atayınca TypeScript 
hatası verdi:
  Type 'string' is not assignable to type '{ [key: string]: string; }'

Sentry SDK'da event.request.cookies object tip — string yerine 
delete kullanmak doğrusu.

✱ DÜZELTME
──────────
  Önce:  event.request.cookies = '<redacted>';
  Sonra: delete event.request.cookies;

Aynı zamanda Authorization/Cookie header'ları da büyük harfli 
versiyonlarıyla beraber silindi (case sensitivity için).

✱ DOSYA (1 adet)
─────────────────
  sentry.server.config.ts

✱ PUSH
──────
  git add sentry.server.config.ts
  git commit -m "fix(sentry): server config type error"
  git push

═══════════════════════════════════════════════════════════════
