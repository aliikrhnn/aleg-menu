ALEG TUR 1 — Test ekibi bug raporu fix paketi (29 Nisan 2026)
================================================================

Bu paket 6 bug'ı çözer (12 bug'dan ilki):

✅ #1  — Onboarding "QR ile test et" 404 → akıllı yönlendirme + güzel modal
✅ #2  — Varyasyon eklendi mesajı "HATA" toast → "BAŞARILI" toast
✅ #3  — Kasiyer yokken Kasa 404 → güzel empty state + yönlendirme
✅ #4  — "Cannot read properties of null (reading 'tr')" hatası — null safe i18n
✅ #7  — Hızlı Satış'ta menü yüklenmiyor (bug #4'ün ikincil etkisi - aynı fix)
✅ #11 — "Kasiyerler" → "Personeller" (sidebar label)
✅ #12 — İstasyon ekranları bar/mutfak/pastane açılmıyor — sipariş yokken bile stations dönüyor


KURULUM
-------

Bu zip'i aç, içindeki 7 dosyayı projendeki AYNI yerlere kopyala 
(üstüne yaz, "Yes to All"):

  lib/actions/tables-status.ts                                    (bug #4 + #7)
  lib/actions/kds.ts                                              (bug #12)
  app/panel/(shell)/menu/varyasyonlar/presets-manager.tsx         (bug #2)
  components/panel/nav-config.ts                                  (bug #11)
  app/panel/(shell)/dashboard/onboarding.tsx                      (bug #1)
  app/kasa/page.tsx                                               (bug #3)
  app/kasa/kasa-empty-state.tsx                                   (bug #3 - YENİ DOSYA)


PUSH
----

  git add .
  git commit -m "fix: tur 1 - 6 bugfix from test team report"
  git push origin main


KALAN BUG'LAR (TUR 2'ye)
------------------------

❓ #5a — Müşteri telefon: sipariş takibi (yeni özellik)
❓ #5b — Müşteri telefon: ürün değerlendirme (yeni özellik, reviews tablosu mevcut)
❓ #6  — Gün sonu PDF saat grafik (boş saatleri doldur, görsel iyileştir)
❓ #8  — Yazıcı agent ZIP indirme (static dosya + UI)
❓ #9  — Yetkisiz kasiyer kasa erişimi (replication adımı netleşmeli)
❓ #10 — Kasa hızlı satışta cari ödeme (POS akışına cari select ekle)


NOTLAR
------

* tables-status.ts'te yeni `pickLocalized` helper fonksiyonu eklendi.
  i18n alanları (name, description) için null-safe okuma sağlar.
  Bu helper'ı diğer dosyalarda da kullanmak isteyebilirsin.

* kasa-empty-state.tsx YENİ bir dosya — projede daha önce yoktu.
  app/kasa/ klasörüne ekle.

* nav-config.ts'te sadece sidebar label değişti. Sayfa içindeki
  "Kasiyer & Garson" başlığı (cashier-manager.tsx) AYNEN KALDI çünkü
  rol bağlamı korunmalı (kasiyer rolü, garson rolü ayrı).

* Bug #1 için: masa varsa subdomain'e gider, yoksa illüstrasyonlu modal
  açar. Modal "Masa eklemeye git" CTA'sıyla /panel/masalar'a yönlendirir.
