═══════════════════════════════════════════════════════════════
  ALEG · LINT FIX (Push engelleyen 5 hata)
  2 Mayıs 2026
═══════════════════════════════════════════════════════════════

✱ NE DÜZELTİLDİ
────────────────
1. identity-tab.tsx satır 530-532 — JSX'te tırnak/apostrof
   karakterleri escape edilmedi:
   - "WiFi" → &quot;WiFi&quot;
   - QR'ı  → QR&apos;ı

2. hesap-panel.tsx satır 626 ve 760 — useCallback dependency
   listesinde gereksiz `selectedFlatItems` vardı (callback
   gövdesinde kullanılmıyordu, itemRollups zaten ondan türemiş).

✱ DOSYALAR (2 adet)
───────────────────
  app/panel/(shell)/ayarlar/tabs/identity-tab.tsx
  components/order/hesap-panel.tsx

✱ NPM / MIGRATION
─────────────────
GEREKMİYOR.

✱ PUSH (Windows PowerShell)
───────────────────────────
KOLAY YOL:
  git add .
  git status
  git commit -m "fix(lint): jsx escape + useCallback deps temizle"
  git push

VEYA tek tek:
  git add 'app/panel/(shell)/ayarlar/tabs/identity-tab.tsx'
  git add components/order/hesap-panel.tsx
  git commit -m "fix(lint): jsx escape + useCallback deps temizle"
  git push

Pre-push hook'taki next lint artık temiz geçer.

✱ ROLLBACK
──────────
git revert HEAD
git push

═══════════════════════════════════════════════════════════════
