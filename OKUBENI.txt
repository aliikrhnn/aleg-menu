ALEG TUR 9b — ESLint hata düzeltmesi
======================================

SORUN
-----

Pre-push lint hatası:
  'showDietaryTags' is defined but never used. Allowed unused args must match /^_/u.
  'showSinceBadge' is defined but never used.
  'customSignature' is defined but never used.

Sebep: Tür 9'da BoldBadge ve PhotoHeroCategoryBlock fonksiyonlarında bu
prop'lar destructure ediliyordu ama içeride kullanılmıyordu.

ÇÖZÜM
-----

Kullanılmayan prop'ları:
  • PhotoHeroCategoryBlock: tipten ve destructure'dan kaldırıldı
  • BoldBadgeLayout: _ prefix ile rename edildi (TS optional unused)
  • PhotoHeroLayout: showDietaryTags için _ prefix eklendi

ESLint kuralı `argsIgnorePattern: /^_/` olduğu için _ ile başlayan
parametreler ignore edilir.

KOD ETKISI: HİÇ YOK
Tür 9'un tüm görsel davranışı aynı, sadece kullanılmayan prop'lar
ESLint'e "biliyorum, kullanmıyorum" sinyali ile işaretlendi.


KURULUM
=======

1 dosya değiş:

  lib/printable-menu/print-menu-document.tsx (üzerine yaz)


PUSH
====

  git add lib/printable-menu/print-menu-document.tsx
  git commit -m "fix: tur 9b - eslint unused vars in layout components"
  git push origin main


DİĞER TUR 9 DOSYALARINI TEKRAR YÜKLEMENE GEREK YOK.
Sadece bu bir dosya yeterli.
