Paket 4 - BUILD FIX 2 (TÜM SORUNLAR DÜZELTİLDİ)
================================================

Bu paket önceki Paket 4 fix'in üstüne gelir. Aşağıdaki TÜM
TypeScript build hatalarını düzeltir:

1. SerifTitle: 'as' prop'u yok → kaldırıldı
2. StatusDot: 'gold' ve 'olive' kabul etmiyor → ayrı 
   STATUS_DOT_TONE map'leri ile sadeleştirildi
3. FilterChip: children değil 'label' + 'value' alıyor 
   → tüm 13 kullanım yeniden yazıldı

Ek olarak (önceki fix'in tekrarı, garanti olsun diye):
- requireSuperAdmin() her dosyaya gömüldü
- log_audit RPC çağrısı düzgün

YAPMAN GEREKEN:
---------------

Bu zip'i aç ve içindeki TÜM dosyaları projendeki AYNI
yerlere kopyala (üstüne yaz - confirm "Yes to All"):

components/admin/admin-users-client.tsx     ← güncellendi
components/admin/audit-client.tsx            ← güncellendi
components/admin/notifications-client.tsx    ← güncellendi
components/admin/settings-client.tsx         ← güncellendi
components/admin/support-detail-client.tsx   ← güncellendi
components/admin/support-list-client.tsx     ← güncellendi
components/admin/system-client.tsx           ← güncellendi

lib/actions/admin-support.ts          (önceki fix ile aynı)
lib/actions/admin-notifications.ts    (önceki fix ile aynı)
lib/actions/admin-users.ts            (önceki fix ile aynı)
lib/actions/admin-audit.ts            (önceki fix ile aynı)
lib/actions/admin-system.ts           (önceki fix ile aynı)
lib/actions/admin-settings.ts         (önceki fix ile aynı)

app/admin/(shell)/destek/[id]/page.tsx       ← (shell), zip'te (admin)
app/admin/(shell)/kullanicilar/page.tsx       ← (shell), zip'te (admin)

Sonra:
  git add .
  git commit -m "fix: align paket 4 with primitives signatures"
  git push origin main

Bu sefer geçecek.
