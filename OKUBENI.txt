ALEG TUR 2B — Bug #5 + #10 fix paketi
======================================

Bu paket 3 bug'ı çözer:

✅ #5a — Müşteri telefon ekranında sipariş takibi (yeni özellik)
✅ #5b — Müşteri ürün değerlendirme (yeni özellik, reviews tablosu mevcut)
✅ #10 — Kasa hızlı satışta cari ödeme

KALAN: #9 (yetkisiz kasiyer kasa erişimi) — replication adımları lazım


KURULUM (kod tarafı)
--------------------

Bu zip'i aç, içindeki dosyaları projendeki AYNI yerlere kopyala (üstüne yaz):

  lib/actions/orders.ts                                         (#5a - getOrderTracking eklendi)
  app/menu/[slug]/cart-drawer.tsx                               (#5a - "Siparişi takip et" butonu)
  app/menu/[slug]/menu-view.tsx                                 (#5a - businessSlug prop geçişi)
  app/menu/[slug]/siparis/[orderId]/page.tsx                    (#5a + #5b - YENİ DOSYA)
  app/menu/[slug]/siparis/[orderId]/tracking-client.tsx         (#5a + #5b - YENİ DOSYA)
  app/kasa/kasa-board.tsx                                       (#10 - hideOnAccount kaldırıldı)


DEĞİŞİKLİK ÖZETİ
================

#5a — Müşteri sipariş takibi
----------------------------
ESKİ: Müşteri sipariş verdi, "#42" kodunu gördü, hepsi bu. Siparişin
      durumu (alındı/hazırlanıyor/hazır/teslim) takip edilemiyordu.

YENİ:
  • Sepet success ekranına "Siparişi takip et" butonu eklendi
  • Yeni route: /menu/[slug]/siparis/[orderId]
  • 4 adımlı progress bar (alındı → hazırlanıyor → hazır → teslim)
  • Aktif adım vurgulanır (pulse animasyonu)
  • 5 saniyede bir otomatik güncelleme (polling)
  • Sipariş kalemleri + toplam görünür
  • İptal edilmiş siparişler için ayrı durum kartı
  • Brutalist Spice tasarım dili — italic serif, mono eyebrow


#5b — Müşteri ürün değerlendirme
--------------------------------
ESKİ: Reviews tablosu vardı, panel arayüzü vardı, ama müşteri telefondan
      değerlendirme yapamıyordu — UI yoktu.

YENİ:
  • Sipariş "delivered" olunca takip ekranında değerlendirme formu açılır
  • 5 yıldızlı interactive rating (hover efekti)
  • İsteğe bağlı yorum, ad, telefon
  • submitReview server action'ı zaten vardı, sadece UI bağlandı
  • Aynı sipariş için tekrar değerlendirme engelleniyor (server tarafı + UI)
  • Submit edilince teşekkür mesajı


#10 — Hızlı satışta cari ödeme
------------------------------
ESKİ: Hızlı satış ekranında Nakit / Kart / İndirim / Parçalı butonları
      vardı, ama Cari (on_account) butonu kasten gizliyordu (hideOnAccount).

YENİ:
  • Tek satır değişiklik: kasa-board.tsx satır 989 - "hideOnAccount" kaldırıldı
  • Hızlı satışta artık 3 ödeme yöntemi: Nakit / Kart / Cari
  • Cari seçilince zaten mevcut olan CustomerPicker modal açılıyor
  • Müşteri seçimi sonrası borç cari hesaba yazılıyor
  • Gün sonu raporunda görünür (zaten çalışan akış)


PUSH
----

  git add .
  git commit -m "fix: tur 2b - order tracking, reviews, on-account quick sale"
  git push origin main


KALAN BUG (#9)
==============

❓ #9 — Yetkisi olmayan kasiyer kasa kısmındaki kasaya girebiliyor

Bunu çözmek için önce nasıl reproduce edilebileceğini öğrenmem lazım:

  • Kasiyer rolü 'cashier' iken giriş yapıyor, sonra rolü 'waiter'
    yapılıyor mu? Eski session ile devam ediyor mu?
  • Yoksa direkt rol filtresi atlanıyor mu?
  • PIN'siz kasiyer eklemek mümkün mü?

Test ekibinden tam adımları al, ben de fix'leyeyim.


YENİ DOSYA YAPISI
=================

app/menu/[slug]/
├── cart-drawer.tsx        (güncellendi)
├── menu-view.tsx          (güncellendi)
├── siparis/               (YENİ KLASÖR)
│   └── [orderId]/         (YENİ KLASÖR)
│       ├── page.tsx       (YENİ DOSYA - server component)
│       └── tracking-client.tsx  (YENİ DOSYA - client component)
└── ... (diğerleri değişmedi)
