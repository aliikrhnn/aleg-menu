# Public klasörü

Bu klasöre şunlar eklenecek:
- `favicon.ico` — tarayıcı sekme ikonu
- `logo.svg` — ana logo
- `icons/` — PWA ikonları (192x192, 512x512)
- `manifest.json` — PWA manifest (sonraki fazda)

Şu an boş, sonra dolduracağız.
